<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>net.vetcafe</groupId>
    <artifactId>jtetris</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>JTetris</name>
    <description>A lightweight Java Swing Tetris clone for learning and code-quality practice.</description>
    <url>https://github.com/sleetdrop/jtetris</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/license/mit</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>sleetdrop</id>
            <name>Yuan Jiang</name>
            <email>sleetdrop@gmail.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/sleetdrop/jtetris.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/sleetdrop/jtetris.git</developerConnection>
        <url>https://github.com/sleetdrop/jtetris</url>
    </scm>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jpackage.name>JTetris</jpackage.name>
        <jpackage.type>APP_IMAGE</jpackage.type>
        <jpackage.appVersion>1.0.0</jpackage.appVersion>
        <jpackage.icon>${project.basedir}/art/icon.icns</jpackage.icon>
        <jpackage.macPackageIdentifier>net.vetcafe.jtetris</jpackage.macPackageIdentifier>
        <jpackage.vendor>Yuan Jiang</jpackage.vendor>
        <junit.jupiter.version>5.10.2</junit.jupiter.version>
        <flatlaf.version>3.4.1</flatlaf.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.formdev</groupId>
            <artifactId>flatlaf</artifactId>
            <version>${flatlaf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>net.vetcafe.jtetris.ui.TetrisFrame</mainClass>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>copy-runtime-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeScope>runtime</includeScope>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <mainClass>net.vetcafe.jtetris.ui.TetrisFrame</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>mac</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.4.0</version>
                        <executions>
                            <execution>
                                <id>stage-jpackage-input</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${project.build.finalName}.jar</include>
                                                <include>lib/**</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                            <execution>
                                <id>stage-jpackage-resources</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-resources</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.basedir}/packaging/macos</directory>
                                            <filtering>true</filtering>
                                            <includes>
                                                <include>Info.plist</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>1.7.4</version>
                        <executions>
                            <execution>
                                <id>jpackage-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>${jpackage.type}</type>
                                    <input>${project.build.directory}/jpackage-input</input>
                                    <destination>${project.build.directory}/dist</destination>
                                    <removeDestination>true</removeDestination>
                                    <name>${jpackage.name}</name>
                                    <mainClass>net.vetcafe.jtetris.ui.TetrisFrame</mainClass>
                                    <mainJar>${project.build.finalName}.jar</mainJar>
                                    <appVersion>${jpackage.appVersion}</appVersion>
                                    <vendor>${jpackage.vendor}</vendor>
                                    <icon>${jpackage.icon}</icon>
                                    <resourceDir>${project.build.directory}/jpackage-resources</resourceDir>
                                    <macPackageIdentifier>${jpackage.macPackageIdentifier}</macPackageIdentifier>
                                    <javaOptions>
                                        <option>-Xmx512m</option>
                                        <option>-Dflatlaf.useNativeLibrary=false</option>
                                    </javaOptions>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
