# M7.2 Line Clear Flash Tuning

## Spec ID
- `M7.2-LINE-CLEAR-FLASH-TUNING`

## Status
- `Done`

## Scope
- Goal:
  - Make line-clear flash duration/alpha tunable and improve readability on smaller cell sizes without changing gameplay state or timing.
- File allowlist (exact paths):
  - `doc/specs/m7.2-line-clear-flash-tuning.md`
  - `src/main/java/net/vetcafe/jtetris/ui/GamePanel.java`
  - `doc/overview.md`
  - `doc/specs/context-pack.md`

## Out of scope
- Any changes to `Board` line-clear logic or metadata APIs.
- New settings UI for visual effects.
- Changes to gravity, DAS/ARR, lock delay, or scoring.

## Design notes
- Keep defaults visually close to existing flash behavior.
- Expose tuning via JVM properties so users can tweak without code edits.
- Add bounded parsing and fallback defaults to avoid invalid values breaking rendering.

## Implementation checklist
- [x] Step 1: Add bounded flash timing/alpha property parsing in `GamePanel`.
- [x] Step 2: Add small-cell alpha boost so flash remains visible when board cells are tiny.
- [x] Step 3: Update docs with supported properties and defaults.

## Acceptance criteria
- Flash still works with default settings and remains non-blocking.
- Users can override flash duration/alpha via JVM properties.
- Small cell sizes show visibly stronger flash than before.

## Verification
- Commands:
  - `mvn clean test`
- Result:
  - `Pass (2026-06-03): 39 tests, 0 failures, 0 errors`

## Rollback plan
- Revert this spec allowlist as one changeset to restore fixed flash constants.

## Session handoff draft
- Active spec: `doc/specs/m7.2-line-clear-flash-tuning.md`
- Goal in one line: make line-clear flash tunable and keep visibility on small cells without gameplay changes.
- Current status: `Done`
- Next smallest step: begin `M7.3` with a draft spec for replay persistence API/storage boundaries.
- Risks/blockers: no in-app settings UI yet; tuning remains JVM-arg driven.

