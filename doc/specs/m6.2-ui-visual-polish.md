# M6.2 UI Visual Polish

## Spec ID
- `M6.2-UI-VISUAL-POLISH`

## Status
- `Done`

## Scope
- Goal:
  - Polish light-theme readability and dialog ergonomics based on screenshot feedback while preserving the M6.1 theme architecture.
- File allowlist (exact paths):
  - `doc/specs/m6.2-ui-visual-polish.md`
  - `src/main/java/net/vetcafe/jtetris/ui/UiTheme.java`
  - `src/main/java/net/vetcafe/jtetris/ui/ColorPalette.java`
  - `src/main/java/net/vetcafe/jtetris/ui/GamePanel.java`
  - `src/main/java/net/vetcafe/jtetris/ui/TetrisFrame.java`
  - `doc/specs/context-pack.md`

## Out of scope
- Gameplay rule/timing/input behavior changes.
- New settings UI for runtime theme switching.
- Non-UI refactors and dependency updates.

## Design notes
- Fix critical button contrast issue first (secondary button labels must always remain visible).
- Use content-aware sizing for leaderboard dialog to avoid oversized empty layout.
- Increase light-theme board/readability contrast without removing the classic warm palette direction.

## Implementation checklist
- [x] Step 1: Adjust dialog button text/spacing and leaderboard sizing behavior.
- [x] Step 2: Tune light-theme board/grid/ghost contrast and tetromino edge readability.

## Acceptance criteria
- `No/Cancel` text remains clearly visible in light-theme dialogs.
- Leaderboard dialog scales to content with a bounded viewport instead of filling with empty space.
- Light-theme board elements (grid, ghost, piece edges) are easier to distinguish.

## Verification
- Commands:
  - `mvn clean test`
- Result:
  - `Pass (2026-05-15): 38 tests, 0 failures, 0 errors`

## Rollback plan
- Revert this spec allowlist as one changeset to restore M6.1 visuals.

