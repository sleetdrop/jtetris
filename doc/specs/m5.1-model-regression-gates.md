# M5.1 Model Regression Gates Spec

## Spec ID
`M5.1-MODEL-REGRESSION-GATES`

## Status
`Done`

## Scope
- Add focused regression gates for model-layer correctness.
- Cover collision constraints, rotation failure guard, line-clear state updates, and top-out on blocked spawn.
- Keep runtime gameplay behavior unchanged.

## Out of scope
- Replay recording/restore format.
- CI pipeline wiring.
- UI integration tests.

## Design notes
- Use deterministic model tests (`Board(seed)`) and targeted reflection for private-path validation.
- Prioritize invariants that are historically regression-prone: wall collisions, spawn validity, and clear-state bookkeeping.
- Keep assertions compact and behavior-oriented.

## Implementation checklist
- [x] Add collision regression tests (boundary + occupied-cell movement rejection).
- [x] Add rotation failure gate test for fully blocked board context.
- [x] Add line-clear state consistency test (lines/score/event/grid shift).
- [x] Add top-out spawn-blocking test.
- [x] Update roadmap/context/docs.

## Acceptance criteria
- Regression suite fails on model behavior drift in the covered areas.
- Existing specs remain green after adding new gates.
- Build and test complete successfully.

## Verification
- Manual: N/A (model test focused).
- Automated: `BoardRegressionGateTest` + full `mvn clean test`.

## Rollback plan
- Revert new regression test file if false positives appear.
- Keep production model code unchanged during rollback.

