# M6.3 Line Clear Flash

## Spec ID
- `M6.3-LINE-CLEAR-FLASH`

## Status
- `Done`

## Scope
- Goal:
  - Add a simple LCD-style flash feedback when line clear happens, using only Java standard Swing capabilities.
- File allowlist (exact paths):
  - `doc/specs/m6.3-line-clear-flash.md`
  - `src/main/java/net/vetcafe/jtetris/model/Board.java`
  - `src/main/java/net/vetcafe/jtetris/ui/GamePanel.java`
  - `src/test/java/net/vetcafe/jtetris/model/BoardRegressionGateTest.java`
  - `doc/overview.md`
  - `doc/specs/context-pack.md`

## Out of scope
- Changes to gameplay timing, lock delay, gravity, or input behavior.
- Theme system architecture changes or runtime settings UI.
- Non-line-clear effects (particles/sound/shaders).

## Design notes
- Keep line clear logic deterministic and fast; expose only minimal metadata needed by UI for effect.
- Render flash as a short, alternating translucent row overlay (low-cost, no extra dependencies).
- Effect must be non-blocking and must not alter game state progression.

## Implementation checklist
- [x] Step 1: Emit cleared-row metadata from model and render short row-flash overlay in `GamePanel`.

## Acceptance criteria
- Clearing at least one line triggers a visible brief flash on the cleared row area.
- Effect is lightweight and does not pause gameplay progression.
- Existing model tests remain passing.

## Verification
- Commands:
  - `mvn clean test`
- Result:
  - `Pass (2026-05-15): 39 tests, 0 failures, 0 errors`

## Rollback plan
- Revert this spec allowlist as one changeset to remove flash effect and restore prior visuals.
